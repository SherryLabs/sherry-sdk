# Sherry SDK Introduction

[![npm version](https://img.shields.io/npm/v/@sherrylinks/sdk.svg)](https://www.npmjs.com/package/@sherrylinks/sdk)
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://opensource.org/licenses/MIT)
[![TypeScript](https://img.shields.io/badge/TypeScript-4.9%2B-blue)](https://www.typescriptlang.org/)
[![Tests](https://img.shields.io/badge/Tests-Jest-green)](https://jestjs.io/)
[![GitHub Repo](https://img.shields.io/badge/GitHub-Repo-blue.svg)](https://github.com/SherryLabs/sherry-sdk)

## 🌟 Overview

import Admonition from '@theme/Admonition';

<Admonition type="caution" title="Disclaimer">
  Our **SDK** and documentation are still under development; we're working hard to ensure everything
  is as polished as possible. Any feedback is very helpful — you can contact us on
  [**Discord**](https://discord.gg/69brTf6J)
</Admonition>

---

Sherry SDK is a powerful toolkit for building interactive Web3 **mini-apps** that can be embedded directly within social media posts. The SDK enables developers to create rich, composable blockchain experiences without requiring users to leave their social media feed.

With Sherry, you can transform any post into an interactive dApp that allows users to swap tokens, vote on proposals, mint NFTs, sign transactions, transfer assets, interact with APIs, and much more—all with built-in validation and a unified experience across multiple blockchains.

---

## ✨ Key Features

- 🔗 **Multi-chain Support**: Build once, deploy across Avalanche, Celo, Ethereum, and more.
- 🧩 **Multiple Action Types**:
  - **Transfer Actions**: Enable native token transfers with customizable UIs.
  - **Blockchain Actions**: Call smart contract functions with rich parameter configuration.
  - **Dynamic Actions**: Create flexible mini-apps that compute actions server-side.
  - **Action Flows**: Create interactive multi-step processes with conditional paths using `decision` and `completion` steps.
- 📋 **Built-in Validation**: Ensures your mini-app metadata is valid and well-formed before use via `createMetadata` or `validateMetadata`.
- ⚡ **Type Safety**: Full TypeScript support with comprehensive type definitions.
- 🔄 **Cross-chain Interactions**: Enable transactions across multiple blockchains via Wormhole integration.
- 📊 **Metadata Templates**: Ready-to-use templates for common Web3 use cases.

---

## 🚀 Getting Started

Ready to build your first mini-app? Here are the main paths:

### Quick Start Options

1. **[Create Your First Mini-App](getting-started/creatingminiapp)** - Start with the basics
2. **[View Examples](getting-started/examples.md)** - See working examples
3. **[Follow the Complete Guide](guides/guide-en.md)** - Step-by-step tutorial

### Core Concepts

- **[Understanding Actions](api/action-types)** - Learn about different action types
- **[Metadata Structure](core-concepts/metadata.md)** - Understand the mini-app definition format
- **[Chain Support](core-concepts/chains)** - Multi-chain and cross-chain capabilities

---

## 👀 Who is this SDK for?

This SDK is aimed at developers who:

- Want to integrate blockchain functionalities into social platforms or web contexts.
- Seek to create interactive Web3 experiences without users leaving their current environment.
- Need to build multi-step workflows with conditional logic.
- Value a tool with integrated validation and strong typing.

---

## 📚 What's Next?

Choose your learning path:

import { useColorMode } from '@docusaurus/theme-common';

export const QuickLinks = () => {
  const { colorMode } = useColorMode();
  
  const links = [
    {
      title: "🏗️ Build Your First Mini-App",
      href: "getting-started/creatingminiapp",
      description: "Learn the basics with a simple example",
      time: "5 min"
    },
    {
      title: "🎯 Explore Action Types", 
      href: "api-reference/action-types/blockchain-actions",
      description: "Understand different ways to interact with blockchain",
      time: "10 min"
    },
    {
      title: "📖 Complete Guide",
      href: "guides/guide-en",
      description: "Step-by-step tutorial with Next.js",
      time: "30 min"
    },
    {
      title: "💡 View Examples",
      href: "/sdk/examples",
      description: "Ready-to-use examples for common use cases",
      time: "2 min"
    }
  ];

return (
  <>
    {/* Add embedded style tag with CSS classes */}
    <style>
      {`
        .quick-link-card {
          display: block;
          padding: 1.5rem;
          border-radius: 8px;
          text-decoration: none;
          color: inherit;
          transition: all 0.3s ease;
          border: ${colorMode === 'dark' ? '1px solid #444' : '1px solid #e0e0e0'};
          background-color: ${colorMode === 'dark' ? '#2d2d30' : '#f9f9f9'};
        }
        
        .quick-link-card:hover {
          transform: translateY(-5px);
          box-shadow: ${colorMode === 'dark' ? '0 8px 20px rgba(255,255,255,0.15)' : '0 8px 20px rgba(0,0,0,0.1)'};
          border-color: var(--ifm-color-primary);
        }
        
        .card-title {
          font-size: 1.2rem;
          font-weight: bold;
          margin-bottom: 0.5rem;
        }
        
        .card-description {
          font-size: 0.9rem;
          color: ${colorMode === 'dark' ? '#b3b3b3' : '#666'};
          margin-bottom: 0.5rem;
        }
        
        .card-time {
          font-size: 0.8rem;
          color: var(--ifm-color-primary);
          font-weight: 500;
        }
      `}
    </style>

    <div
      style={{
        display: 'grid',
        gridTemplateColumns: 'repeat(auto-fit, minmax(300px, 1fr))',
        gap: '1.5rem',
        margin: '2rem 0',
      }}
    >
      {links.map((link, idx) => (
        <a
          key={idx}
          href={link.href}
          className="quick-link-card"
        >
          <div className="card-title">
            {link.title}
          </div>
          <div className="card-description">
            {link.description}
          </div>
          <div className="card-time">
            {link.time}
          </div>
        </a>
      ))}
    </div>
  </>
); };

<QuickLinks />

---

## 🔧 Installation

```bash
# Using npm
npm install @sherrylinks/sdk

# Using yarn
yarn add @sherrylinks/sdk
```

## 📝 Quick Example

```typescript
import { createMetadata, Metadata } from '@sherrylinks/sdk';

const metadata: Metadata = {
  url: 'https://myapp.example',
  icon: 'https://example.com/icon.png',
  title: 'Token Transfer',
  description: 'Send tokens easily',
  actions: [
    {
      type: 'transfer',
      label: 'Send 0.1 AVAX',
      to: '0x1234567890123456789012345678901234567890',
      amount: 0.1,
      chains: { source: 'avalanche' },
    },
  ],
};

// Validate and use
const validated = createMetadata(metadata);
```

---

Explore the SDK documentation to get started with your Web3 mini-apps!
